# Crea un programa que imprima en pantalla todos los números enteros desde 0 hasta 100 (incluyendo ambos extremos), en orden creciente, mostrando un número por línea.
for i in range (0, 101):
    print(i)

# con WHILE quedaria asi

numero = 0

while numero <= 100:
    print (numero)
    numero += 1

# Desarrolla un programa que solicite al usuario un número entero y determine la cantidad de dígitos que contiene.

numero = int(input("Ingresa un numero entero "))
contador = 0
while numero > 0:
    numero = numero // 10
    contador += 1 
print(contador)

# 3- Escribe un programa que sume todos los números enteros comprendidos entre dos valores dados por el usuario, excluyendo esos dos valores.

numero1 = int(input("Ingresa el primer numero entero "))
numero2 = int(input("Ingresa el segundo numero entero "))
suma = 0
for i in range(numero1 +1, numero2):
    suma += i
print(suma)

# 4 Elabora un programa que permita al usuario ingresar números enteros y los sume en secuencia. El programa debe detenerse y mostrar el total acumulado cuando el usuario ingrese un 0.
suma = 0
numero = int(input("Ingresa un número entero: "))

while numero != 0:
    suma += numero
    numero = int(input("Ingresa otro número entero: "))

print("La suma total es:", suma)

# 5- Crea un juego en el que el usuario deba adivinar un número aleatorio entre 0 y 9. Al final, el programa debe mostrar cuántos intentos fueron necesarios para acertar el número.

import random
numeroalazar = random.randint (0, 9)
contador = 0

numero = int(input("Debes adivinar que numero saldra. Ingresa un numero entrero entre 0 y 9: "))

while numero != numeroalazar:
    contador += 1
    print("el numero", numero, "no es correcto intenta de nuevo: ")
    print("intentos efectuados", contador)
    numero = int(input("Ingresa otro número entre 0 y 9: "))

numero = numeroalazar
print("acertaste! el numero", numero, "es el correcto")

# 6 - Desarrolla un programa que imprima en pantalla todos los números pares comprendidos entre 0 y 100, en orden decreciente.

for i in range (100, -1, -2):
    print(i)

# 7- Crea un programa que calcule la suma de todos los números comprendidos entre 0 y un número entero positivo indicado por el usuario.

numero1 = 0
numero2 = int(input("Ingresa un numero entero "))
suma = 0
for i in range(numero1, numero2 +1):
    suma += i
print(suma)

# 8 - Escribe un programa que permita al usuario ingresar 100 números enteros......

cantidad = 100
ingresados = 0

par = 0
impar = 0
negativos = 0
positivos = 0

while ingresados < cantidad:
    
    numero = int(input(f"Ingrese un número: "))
    ingresados += 1

    if numero % 2 == 0:
        par += 1
    else:
        impar += 1

    if numero > 0:
        positivos += 1
    elif numero < 0:
        negativos += 1

print("Cantidad de pares:", par)
print("Cantidad de impares:", impar)
print("Cantidad de positivos:", positivos)
print("Cantidad de negativos:", negativos)

# 9 - Elabora un programa que permita al usuario ingresar 100 números enteros y luego calcule la media de esos valores.

cantidad = 3
ingresados = 0
suma = 0

while ingresados < cantidad:
    
    numero = int(input(f"Ingrese un número: "))
    ingresados += 1
    suma += numero

media = suma / cantidad 

print("la media de los numeros ingresados es", media)

# 10 - Escribe un programa que invierta el orden de los dígitos de un número ingresado por el usuario. Ejemplo: si el usuario ingresa 547, el programa debe mostrar 745.

numero = int(input("Ingresa un número entero: "))
invertido = 0

while numero > 0:
    digito = numero % 10
    invertido = invertido * 10 + digito
    numero = numero // 10

print("Número invertido:", invertido)














